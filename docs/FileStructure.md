# คู่มือ File Structure 

### 📂 Root Files
- **Program.cs** - จุดเริ่มต้นของแอปพลิเคชัน ตั้งค่า ASP.NET Core, Middleware, และ Services
- **appsettings.json** - ไฟล์ตั้งค่า (configuration) เช่น database connection, logging settings
- **appsettings.Development.json** - ตั้งค่าเฉพาะสำหรับสภาพแวดล้อม Development
- **.csproj** - ไฟล์โปรเจค ระบุ dependencies และ target framework

### 📂 Controllers
ควบคุมตรรกะการทำงานของแอปและจัดการ HTTP requests

- **Ex: HomeController.cs** - จัดการหน้า Home, Index, Privacy, Error

### 📂 Models
โครงสร้างข้อมูล (Entities) ของแอปพลิเคชัน
- **Ex : ErrorViewModel.cs** - โมเดลสำหรับแสดงข้อมูลข้อผิดพลาด

### 📂 Views
ไฟล์ Razor HTML สำหรับแสดงผลหน้าเว็บ
- **Home/** - Views สำหรับ HomeController
  - `Index.cshtml` - หน้าแรก
  - `Privacy.cshtml` - หน้า Privacy
- : Shared/** - Views ที่ใช้ร่วมกันทั่วแอป
  - `_Layout.cshtml` - เทมเพลตหลัก (navbar, footer, scripts)
  - `Error.cshtml` - หน้าแสดงข้อผิดพลาด
- **_ViewStart.cshtml** - ตั้งค่าเริ่มต้นสำหรับ Views ทั้งหมด
- **_ViewImports.cshtml** - Import directives ที่ใช้ใน Views

### 📂 wwwroot
ไฟล์ static assets ที่ส่งไปยัง client
- **css/** - ไฟล์ CSS
  - `site.css` - stylesheet หลักของแอป
- **js/** - ไฟล์ JavaScript
  - `site.js` - script หลักของแอป
- **lib/** - Libraries ที่ติดตั้งผ่าน npm/bower

### 📂 Properties
ตั้งค่าสำหรับสภาพแวดล้อมรัน
- **launchSettings.json** - ระบุ port, profile สำหรับรัน debug/release

### 📂 Database 
เก็บไฟล์ที่เกี่ยวกับการจัดการฐานข้อมูล

### 📂 DTO 
เก็บคลาส DTO สำหรับส่งข้อมูลระหว่าง Controller และ Client